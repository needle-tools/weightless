version: 2

project_name: weightless

before:
  hooks:
    - go test ./...

builds:
  - id: weightless
    main: .
    binary: weightless
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.Version={{ .Version }}

archives:
  - id: weightless
    ids:
      - weightless
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

release:
  github:
    owner: hybridherbst
    name: weightless

brews:
  - name: weightless
    ids:
      - weightless
    homepage: https://github.com/hybridherbst/weightless
    description: Find local AI model weights across apps, caches, and project folders.
    license: MIT
    repository:
      owner: hybridherbst
      name: homebrew-tap
      branch: main
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    directory: Formula
    test: |
      system "#{bin}/weightless", "--version"
